# Chat Web

Next.js web client for the chat application.

## Local Development

```bash
npm install
cp .env.example .env
npm run dev
```

The web app runs at `http://localhost:3000` in local development.

## Docker Compose

From the repository root:

```bash
docker compose up -d
```

The web app is available at `http://localhost:3000`.
